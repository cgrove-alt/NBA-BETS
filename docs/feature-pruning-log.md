# Feature Pruning Log — Phase 2, Step 2

**Date:** 2026-02-23
**Status:** Infrastructure built, awaiting full audit run

## Infrastructure Added

1. **`scripts/feature_importance_audit.py`** — SHAP + built-in importance analysis script
2. **`FeatureSelector` integration** in training pipeline (`train_complete_balldontlie.py`)
3. **`filter_features()`** utility in `feature_engineering.py` for inference-time gating
4. **`models/selected_features.json`** — placeholder, populated by audit script or training
5. **`shap>=0.43.0`** added to requirements.txt

## How to Run

```bash
# Install shap
pip install shap>=0.43.0

# Run the audit (requires trained models in models/)
python3 scripts/feature_importance_audit.py --threshold 0.01

# Or with faster built-in importances only (no SHAP)
python3 scripts/feature_importance_audit.py --threshold 0.01 --skip-shap
```

## Current Feature Count

- **147 features** across 13 categories in `feature_engineering.py`
- Target after pruning: **100-120 features** (remove those below 1% cumulative importance)

## Pruning Results

*To be populated after running the audit script against trained models.*

| Metric | Value |
|---|---|
| Original features | 147 |
| Selected features | TBD |
| Pruned features | TBD |
| Method | TBD |
| Backtest ROI (before) | TBD |
| Backtest ROI (after) | TBD |

## Feature Rankings

*Will be auto-generated by `scripts/feature_importance_audit.py`.*
