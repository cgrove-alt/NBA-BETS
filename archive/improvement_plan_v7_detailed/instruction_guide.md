
# Master Instruction Guide: NBA Model Upgrade (v7)

## Overview
This document contains the **Analysis & Implementation Plan** derived from the forensic investigation of the Jan 7th, 2026 model failure. The analysis phase is complete. The "Brain" (Analysis Agent) has identified the root causes and prototyped the necessary feature engineering fixes.

**Your Mission**: Complete the **Phase 3: Model Architecture Upgrade** and **Phase 4: Full Deployment**.

## 1. Analysis Findings (Context)
- **Root Cause**: The failure was caused by a lack of context regarding team possession quality (Four Factors) and injury impact depth (Usage Lost).
- **Fixes Applied (Prototypes)**:
    - `advanced_stats_v2.py`: Implemented Four Factors (eFG%, TOV%, ORB%, FT/FGA).
    - `feature_engineering.py`: Updated to include `usage_lost` and `star_player_out` metrics.

## 2. Execution Plan (Your Tasks)

### Phase A: Stacked Model Architecture (CRITICAL)
The current simple Logistic/RandomForest models are insufficient for capturing the complex interactions between the new advanced features.
- [ ] **Create Stacking Framework**: Implement a 2-layer stacking architecture.
    - **Level 1**: diverse base models (XGBoost, LightGBM, RandomForest, LogisticRegression).
    - **Level 2**: Meta-learner (LogisticRegression or Ridge) to combine predictions.
- [ ] **Implementation File**: Create `models/stacking_model.py`.
- [ ] **Training Script**: Create `train_stacking_model.py` to train this new architecture on the enhanced feature set.

### Phase B: Verification & Tuning
- [ ] **Run Comprehensive Backtest**: Use `comprehensive_backtest.py` (which has been patched to support team predictions) to verify the new architecture's ROI.
- [ ] **Hyperparameter Tuning**: Use Optuna to tune the XGBoost/LightGBM parameters specifically for the 2025-26 season data.

### Phase C: Deployment
- [ ] **Update Daily Prediction Pipeline**: logical updates to `daily_predictions.py` to use the new `.pkl` model files generated by the stacking trainer.
- [ ] **Final Dry Run**: Run predictions for "today" to ensure the pipeline is stable.

## 3. Key Files Reference
- `implementation_plan.md`: The detailed technical spec.
- `tasks.md`: The tracking checklist.
- `advanced_stats_v2.py`: The new stats library (already created).

## 4. Acceptance Criteria (For Verification)
**I will be checking your work against these specific metrics:**
1.  **Stacking Implementation**: `train_complete_balldontlie.py` must successfully train and save a `.pkl` file that contains the Meta-Learner.
2.  **Backtest Success**: `comprehensive_backtest.py` must run without error and show:
    - Moneyline Accuracy > 60% (Baseline was ~55%).
    - Spread MAE < 10.5 points.
3.  **Code Quality**: No hardcoded "magic numbers" in the model architecture; use variables/constants.
4.  **Integration**: The `daily_predictions.py` script must successfully generate a prediction for a test date (e.g., Jan 20, 2026) using the new Stacking Model.
